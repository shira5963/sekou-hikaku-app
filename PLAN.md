# 施工前&施工後比較報告書アプリ 計画書

## 1. アプリ概要

内装リフォーム現場の「施工前」「施工後」の写真を自動でペアリングし、
A4サイズの比較報告書（PDF）を自動生成するアプリ。

- 入力：物件名、施工前フォルダの写真、施工後フォルダの写真（合計25〜40枚程度）
- 出力：A4縦のPDF（1ページに写真ペア5組＝10枚、上部に物件名）

---

## 2. 推奨技術構成と選定理由

| 用途 | 技術 | 選定理由 |
|---|---|---|
| プログラミング言語 | **Python** | 文法がシンプルで初心者向け教材・日本語情報が豊富。今回の「画面」「画像処理」「PDF生成」を全部1言語で完結できる |
| 画面（アップロードUI） | **Streamlit** | HTML/CSS/JavaScriptの知識が不要。数十行のPythonコードだけで「ファイルアップロード」「テキスト入力」「ボタン」を持つWebアプリが作れる |
| PDF生成 | **fpdf2** | 写真の座標（左右2列×5行）や矢印線を数値で細かく指定できるため、今回のような「決まったレイアウトに写真を並べる」用途に最適。reportlabより文法が易しい |
| 画像処理 | **Pillow (PIL)** | 写真の縦横比を保ったままリサイズ・トリミングする際の定番ライブラリ。Streamlit・fpdf2とも相性が良い |

**まとめて選んだ理由：**
すべてPythonの範囲で完結するため、覚える言語・文法が1つで済みます。
Webアプリのフロントエンド（画面）とバックエンド（処理）を別々に学ぶ必要がなく、
「初めてアプリを作る人」が挫折しにくい構成です。ローカルPCで動作確認しながら作り、
慣れてきたらStreamlit Community Cloud（無料）で誰でもアクセスできる形に公開することも可能です。

---

## 3. プロジェクト構成（フォルダ）

```
project/
├─ app.py              ← Streamlitアプリ本体（画面＋処理の呼び出し）
├─ pdf_builder.py       ← PDFレイアウト生成ロジック
├─ requirements.txt     ← 必要ライブラリ一覧
├─ uploads/
│   ├─ 施工前/           ← アップロードされた施工前写真の一時保存先
│   └─ 施工後/           ← アップロードされた施工後写真の一時保存先
├─ output/               ← 生成されたPDFの保存先
└─ PLAN.md               ← 本計画書
```

---

## 4. 写真のアップロード〜自動生成の方法

### ステップ1：物件名を入力
画面上部のテキストボックスに物件名を入力（例：「〇〇マンション101号室」）。

### ステップ2：写真をまとめてアップロード
Streamlitの複数ファイルアップロード機能を使い、
「施工前」「施工後」それぞれの写真をフォルダごとまとめて選択してアップロードします。

### ステップ3：ペアリング（重要）
同じ場所の写真を自動で対応付けるには、**ファイル名を揃えるルール**を採用するのが
初心者には最も確実で実装が簡単です。

- 例：施工前フォルダ「01_リビング.jpg」⇔ 施工後フォルダ「01_リビング.jpg」
- 撮影時にファイル名（または先頭の連番）を同じにしてもらう運用にする
- アプリ側はファイル名でソートし、上から順にペアとして扱う

> ※ AIの画像認識で自動的に「同じ場所」を判定する方法もありますが、
> 精度が不安定で実装・デバッグの難易度が高いため、最初の段階では非推奨です。
> まずはファイル名ルールで確実に動くものを作り、慣れてきたら拡張するのがおすすめです。

### ステップ4：ペア内容の確認（プレビュー）
自動ペアリングされた一覧を画面に表示し、
「このペアで合っているか」を目視確認できるようにします。
ファイル名にズレがあった場合は、ここで手動修正できる仕組みを入れておくと安心です。

### ステップ5：PDF生成ボタン
「報告書を作成」ボタンを押すと、次のレイアウト仕様に沿ってPDFを自動生成します。

### ステップ6：ダウンロード
生成されたPDFをその場でダウンロードできるようにします。

---

## 5. レイアウト仕様（A4縦・1ページ）

```
┌─────────────────────────────┐
│         物件名（上部中央）        │
├───────────────┬───────────────┤
│ 施工前         │  施工後         │
│ [写真1] → [写真1]              │
│ [写真2] → [写真2]              │
│ [写真3] → [写真3]              │
│ [写真4] → [写真4]              │
│ [写真5] → [写真5]              │
└───────────────┴───────────────┘
```

- 1ページ＝ペア5組（施工前5枚＋施工後5枚＝合計10枚）
- 左列上部に「施工前」、右列上部に「施工後」のラベル
- 各ペアの中間に右向き矢印（→）
- 写真が25〜40枚（＝12〜20ペア）ある場合は、5組ごとに自動で改ページ（3〜4ページ構成）

---

## 6. 開発ステップ（ロードマップ）

1. **環境構築**：Pythonをインストールし、`pip install streamlit fpdf2 pillow` で必要ライブラリを導入
2. **画面の実装**：物件名入力・写真アップロードのStreamlit画面を作る
3. **ペアリング処理**：ファイル名でソートしてペア配列を作る関数を作る
4. **PDF生成処理**：fpdf2で写真の座標計算・リサイズ配置・矢印描画・改ページ処理を実装
5. **動作確認**：実際の写真25〜40枚でテストし、ズレやレイアウト崩れを調整
6. **（任意）公開**：Streamlit Community Cloudなどにデプロイし、複数人で使えるようにする

---

## 7. 今後決めたいこと（次の打ち合わせ事項）

- ファイル名の命名ルールを現場担当者に周知する運用フローをどうするか
- 矢印や文字のフォント・デザインの細かい指定（社名ロゴを入れるか等）
- 生成したPDFの保存・共有方法（メール添付／クラウド共有など）
